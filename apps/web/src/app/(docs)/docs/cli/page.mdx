# KhulnaSoft CLI

KhulnaSoft CLI is a command line tool that allows you to list, kill running sandboxes, and manage [sandbox templates](/docs/sandbox-templates).

## Installation

You can install KhulnaSoft CLI using the following command:

<CodeGroup isTerminalCommand>
```bash
npm i -g @khulnasoft/cli
```
</CodeGroup>
